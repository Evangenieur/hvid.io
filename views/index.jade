extends layout

block content
    div#player
        
    div#clickjack

    section#main.animated
        header#header
            h1 
                span #h
                | vid
                span .io
            p.tagline World Wide Watch - Watch our World live

            form#form(onsubmit="return false")
                input#keyword(name='keyword', placeholder='any hashtag or keyword...', autocomplete='off')

        div#results

        div#loading
            div#counter

        div#close

    div#hashtags
        div.arrow
        div.inner
            p 
                | No idea? 
                br
                | Why not try one of there?
            ul 
                li
                    a(href="#christmas") #Christmas
                li
                    a(href="#santaclaus") #SantaClaus
                li
                    a(href="disneyland") Disneyland

    script#videosTemplate(type='text/template')
        |  <ul id="video-list-<%= search %>" class="video-list">
        |  <% _.each(videos, function(video) { %>
        |  <% include videoTemplate %>
        |  <% }) %>
        |  </ul>

    script#videoTemplate(type='text/template')
        |  <li class="video" id="<%= video.dom_id %>">
        |      <div class="video-cell">
        |          <div class="video-wrapper">
        |              <img class="preview" alt="<%= video.title %>" src="<%= video.thumbnail %>">
        |              <a class="play" href="<%= video.embed %>"></a>
        |              <span class="tip"><%= video.score %></span>
        |          </div>
        |          <div class="video-meta">
        |              <h5 title="<%= video.title %>"><%= video.title || 'Unknown title' %></h5>
        |              <ul>
        |                  <li>
        |                       <time datetime="<%= video.date %>"><%= video.date %></time>
        |                       <br> 
        |                       on <%= video.on.join(",") %> from <a class="from" href="<%= video.url %>" target="_blank"><%= video.provider %></a>
        |                   </li>
        |              </li>
        |          </div>
        |      </div>
        |  </li>
